# 2026-03-30

- User wants a dedicated Discord presence for "Лис" with persona/memory separated from the existing service/agent bot.
- Confirmed current OpenClaw config routes all Discord traffic to agent `discord-docs` via a channel-wide binding, so current Discord bot is not the personal main home for Лис.
- Proposed architecture accepted by user: keep existing Discord bot/service path for heartbeat/ACP/service work; create a second dedicated Discord bot/application for Лис; ideally use Discord DMs as the primary chat surface, with optional dedicated server channel later.
- User created a new Discord application/bot for Лис and renamed it to `DomK_Fox`.
- Guided user through Discord Developer Portal setup: enable Message Content Intent and Server Members Intent, disable unnecessary elevated powers, use OAuth2 scopes `bot` + `applications.commands`, and grant least-privilege text/file/reaction/slash-command/voice permissions.
- User wants banner/branding and voice support for the dedicated Лис bot.
- Initial Discord identifiers collected during setup: server `1487218606465880276`, user `1212806539933388832`, and dedicated text channel `#fox-chat` id `1488039869103083570`.
- Added second Discord account `fox` in OpenClaw config with env-backed token ref `DISCORD_FOX_BOT_TOKEN` and display name `DomK_Fox`.
- Replaced broad Discord routing with account-scoped bindings: `default -> discord-docs`, `fox -> main`.
- Account-level guild/channel policy for `fox` was added so the bot can answer without mention only for the user's allowlisted guild/user and specifically in `#fox-chat`.
- Live verification succeeded: `openclaw status --deep` showed `Discord OK ok (@DomK_Fox:fox:733ms)`.
- Live chat verification succeeded in Discord: `DomK_Fox` replies in `#fox-chat` and in topic threads as `main`/Лис.
- Working Discord structure now: private text channel `#fox-chat` plus separate topic threads (`OpenClaw`, `Автоматизация`, `Личное`) for isolated discussions with Лис.
- Remaining cleanup noted: rotate the exposed bot token later, then optionally clean config leftovers and add voice support for the same `DomK_Fox` bot.
- Reviewed local OpenClaw docs and current workspace behavior for memory, compaction, and heartbeat design.
- Decided on target direction: memory/recovery must be low-token by default; recovery should run only on real context failure, not every reset or new chat.
- Confirmed practical memory split: `MEMORY.md` = tiny curated index; `memory/YYYY-MM-DD.md` = fact log; `WORKLOG.md` = optional long-task trail; heartbeat = cheap watchdog only.
- Confirmed current local semantic-memory setup is not ready as a hard dependency: `openclaw memory status --deep --json` fails because `node-llama-cpp` is missing.
- User introduced a new design direction: a Discord library should become a curated knowledge layer, likely maintained by an archivist-style agent instead of the main conversational agent.
- Working hypothesis: workspace markdown remains source of truth for active state; Discord library should store curated reference, decisions, playbooks, and progress summaries rather than raw operational memory.
