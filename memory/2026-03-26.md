## Telegram polling stall diagnostics
- Confirmed Telegram user ID from fresh direct evidence: `6343919005`.
- Group chat ID and topic/thread ID remain unproven because repeated Telegram polling stalls prevented reliable event capture.
- `channels.telegram` currently uses polling mode, not webhook.
- Bot token is currently sourced from `~/.openclaw/openclaw.json` config, not environment.
- No explicit webhook settings or polling-specific timeout settings were found in `channels.telegram`; only global `timeoutSec: 120` was present elsewhere in config.
- Fresh log evidence showed repeated Telegram transport/network failures: `ENETUNREACH`, `EHOSTUNREACH`, `UND_ERR_CONNECT_TIMEOUT`, `ETIMEDOUT`.
- Fresh log evidence showed repeated Telegram polling stall/restart cycles around 07:23-07:30 local time, including `Polling stall detected`, `Polling runner stop timed out after 15s`, and restart backoff.
- Despite the stalls, read-only checks later showed intermittent recovery: `openclaw status --deep` and `openclaw channels status --probe` reported Telegram `ok` / `works` in polling mode.
- Working conclusion: likely root cause is unstable network reachability/transport to Telegram Bot API from this host, not group allowlist config.
- Recommendation at cutoff: do not continue group-ID hunting until polling stability is addressed; next requested evidence was `openclaw channels logs`.
- New evidence after cutoff: `openclaw channels logs` and even direct `tail` of `/tmp/openclaw/openclaw-2026-03-26.log` were both polluted by recursive/matryoshka log re-emission, so they are not trustworthy for fresh Telegram group-event capture.
- Clean conclusion from latest direct evidence: Telegram DM/pairing is still proven only for user `6343919005`; no fresh group/supergroup event, negative `chat.id`, or `message_thread_id` has been proven.
- Transport instability remains directly evidenced in the latest log slice: repeated `deleteWebhook failed`, `Telegram webhook cleanup failed`, `Polling stall detected`, `Polling runner stop timed out`, and Telegram network fallback errors (`ENETUNREACH`, `EHOSTUNREACH`, `UND_ERR_CONNECT_TIMEOUT`, `ETIMEDOUT`).
- Best next diagnostic path chosen: stop using the contaminated log channel and query Telegram Bot API `getUpdates` directly (without printing the token) after sending one fresh message in the group, to confirm whether group updates reach the bot at all and to capture exact `chat.id` / optional `message_thread_id` from authoritative data.
